# Example Environment Variables Configuration
# Copy this file to .env.local and update the values as needed

# ================================
# REQUIRED ENVIRONMENT VARIABLES
# ================================
# These variables are required for the application to function properly

# Supabase Configuration (REQUIRED)
# Get these values from: https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_OR_ANON_KEY=your_supabase_anon_key

# Clerk Authentication (REQUIRED)
# Get these values from: https://dashboard.clerk.com/last-active?path=api-keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key
CLERK_SECRET_KEY=your_clerk_secret_key

# ================================
# OPTIONAL ENVIRONMENT VARIABLES
# ================================
# These variables are optional and provide additional functionality

# Google Gemini AI Configuration (OPTIONAL)
# Required only if you want to use AI-powered mood detection features
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key

# Gemini Model Configuration (OPTIONAL)
# Defaults to "gemini-2.5-flash" if not provided
GEMINI_MODEL=gemini-2.5-flash

# Spotify Integration (OPTIONAL)
# Required only if you want Spotify playlist integration features
# Get these from: https://developer.spotify.com/dashboard/applications
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

# Deployment Environment Variables (OPTIONAL)
# These are automatically set by Vercel during deployment
# You typically don't need to set these manually for local development
VERCEL_URL=localhost:3000
VERCEL_ENV=development
VERCEL_PROJECT_PRODUCTION_URL=your_production_url

# Node.js Environment (OPTIONAL)
# Automatically set by Node.js, but can be overridden
# Common values: development, production, test
NODE_ENV=development